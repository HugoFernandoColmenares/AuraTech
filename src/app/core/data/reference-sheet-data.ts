import { IReferenceSheetDto } from "@core/interfaces/IReferenceSheetDto.interface";

/** Empty demo fallback. Live rows come from Supabase when connected. */
export const referenceSheetData: IReferenceSheetDto[] = [];
