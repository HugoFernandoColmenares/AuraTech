/** Default batch size for Supabase bulk upsert operations. */
export const API_BULK_BATCH_SIZE = 500;

/** PostgREST / Supabase default max rows per request (Dashboard → API → Max rows). */
export const POSTGREST_MAX_PAGE_SIZE = 1000;
